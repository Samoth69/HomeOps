---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/refs/heads/main/postgresql.cnpg.io/pooler_v1.json
apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: pooler-session-rw
spec:
  cluster:
    name: postgres

  template:
    metadata:
      labels:
        ingress.samoth.eu/envoy-internal: allow
        ingress.samoth.eu/vmagent: allow
    spec:
      containers:
        - name: pgbouncer
          resources:
            requests:
              cpu: 5m
              memory: 32Mi
            limits:
              memory: 512Mi

  instances: 3
  type: rw
  pgbouncer:
    poolMode: session
    parameters:
      max_client_conn: "1000"
      default_pool_size: "10"
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/refs/heads/main/monitoring.coreos.com/podmonitor_v1.json
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: &name pooler-session-rw
spec:
  selector:
    matchLabels:
      cnpg.io/poolerName: *name
  podMetricsEndpoints:
    - port: metrics
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: pooler-session-rw-pdb
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: pooler
      cnpg.io/poolerName: pooler-session-rw
